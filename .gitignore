# Dependencies
node_modules/

# Build output
public/build/
dist/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# Keep .env.example checked in (negation must come after the pattern)
/!.env.example

# Backend dev data
workers/*/.dev-data/
workers/*/.wrangler/

# Dev server logs (regenerated on every `npm run dev`)
dev.log
*.log

# Salida del pipeline de logo (scripts/build-logo.js). Los iconos que usa la app
# viven en public/. Estos son intermedios y pesan ~16 MB.
# Ya estaban versionados: para sacarlos del repo hace falta además
#   git rm -r --cached robible/
robible/


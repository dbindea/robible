# CLAUDE.md — Manual operativo de RoBible

> Guía para agentes que trabajan en este repo. Léela antes de tocar nada.
> Documentación ampliada en [docs/](docs/). Estado y pendientes en [ROADMAP.md](ROADMAP.md).

## Qué es RoBible

PWA de lectura bíblica multiidioma con lectura acompañada de música, comparación de versiones, índice temático, favoritos, notas y modo offline completo. Frontend Svelte 5 + Vite en Netlify; backend Cloudflare Workers + D1.

- Producción: **https://robible.com** (Netlify)
- API: **https://robible-api.robible.workers.dev** (Cloudflare Workers + D1 `robible-db`)
- Repo: `github.com/dbindea/robible` — rama de trabajo `develop`, producción `master`

## Reglas de trabajo

**Git — regla heredada, sigue vigente:**

- El agente **NO** hace `git commit` / `push` / `PR` / `merge` de archivos del frontend. Se dejan los cambios en el working tree y el usuario los revisa y commitea.
- El agente **sí** gestiona: backend (`workers/`), deploys a Cloudflare, queries a D1, scripts de mantenimiento.
- Nunca trabajar directamente sobre `master`. `master` solo recibe merges hechos por el usuario.

**Alcance:** este proyecto está en producción con usuarios reales. Antes de cambiar comportamiento de auth, service worker o datos bíblicos, avisar del impacto.

## Comandos

| Comando | Qué hace |
|---|---|
| `npm run dev` | Vite en `localhost:5173` (`--host 0.0.0.0`, accesible desde el móvil en la misma red) |
| `npm run build` | `vite build` → `dist/` + `scripts/generate-seo.mjs` (páginas SEO estáticas y sitemaps) |
| `npm run preview` | Sirve el build de `dist/` |
| `npm run lint` | ESLint. **Debe salir en 0 errores.** Quedan 12 avisos deliberados (`svelte/require-each-key`, `infinite-reactive-loop`): son señales reales pero no bloqueantes, no las silencies sin mirarlas |
| `npm run format` | Prettier sobre todo el repo |
| `node workers/robible-api/dev-server.js` | Backend local en `127.0.0.1:8787` (emula Workers+D1 con `node:sqlite`) |
| `node scripts/build-logo.js` | Regenera todos los favicons desde el SVG fuente |
| `node scripts/build-bible-data.mjs [ver]` | Descarga y valida los datos de una versión bíblica (ver `public/data/CREDITS.md`) |
| `node scripts/build-daily-verses.mjs` | Regenera `public/data/daily-verses.json` y valida las referencias contra **todas** las versiones instaladas |
| `node scripts/build-curated-topics.mjs` | Regenera `public/data/curated-topics.json` (las colecciones de `/versete/<slug>`). Valida las referencias contra todas las versiones y los iconos contra `topic-icons.js` |
| `node scripts/generate-vapid-keys.mjs` | Genera el par de claves VAPID de las notificaciones push. **Se ejecuta una sola vez**: cambiarlas invalida todas las suscripciones existentes |
| `node scripts/build-icons.mjs` | Regenera `src/components/Icon.svelte` desde los SVG de Phosphor (ver `public/data/CREDITS.md`) |
| `npm test` | Suite con `node --test` (189 tests, sin dependencias). **En Windows el glob es obligatorio**: `node --test tests` a secas intenta cargar el directorio como módulo y falla |

Node ≥ 24.15.0 (ver `.nvmrc`).

## Convenciones a respetar

- **Svelte 5 en sintaxis legacy**: el código usa `export let` y `$:`, no runes (`$state`, `$derived`). No migrar un archivo a runes de paso; si se hace, es una decisión explícita y completa.
- **Comentarios en castellano**, con separadores del estilo `// ── Sección ──────`. Explican el *porqué*, sobre todo en las guardas anti-race. Mantener ese registro.
- **Nada de texto hardcodeado en UI**: todo pasa por `$_('clave.anidada')` y la clave debe existir en los **cuatro** archivos `public/lang/{ro,es,en,zh}.json`.
- **Servicios de datos**: cualquier entidad nueva sigue el patrón API-first con fallback a `localStorage` vía `withFallback()` de [src/services/apiClient.js](src/services/apiClient.js). Ver `favorites.service.js` como referencia canónica (es el más corto).
- **SCSS**: siempre tokens (`var(--color-accent)`, `var(--color-surface)`…) definidos en `public/global.css`, y **nunca** una regla por paleta en un componente (ver trampa del sistema de diseño). Nunca colores literales sueltos.
- **Tests**: en `tests/*.test.js`, con el runner de Node (`node:test` + `node:assert/strict`). Nada de frameworks. Solo se prueba lógica pura importable sin navegador; lo que toca `localStorage` se dobla con un stub mínimo (ver `filter.test.js`). Si arreglas un fallo, deja antes el test que lo reproduce.
- **Rutas**: se construyen con `buildBiblePath()` / se leen con `parseBiblePath()` de [src/services/bible-route.service.js](src/services/bible-route.service.js). No parsear `pathname` a mano.

## Trampas del repo

Cosas que rompen si no se saben:
1. **El evento de navegación se llama `robibile:navigate`** — con la errata, `bibile` en vez de `bible`. Está así en 10 sitios. Al escuchar o emitir, hay que escribirlo mal a propósito. Renombrarlo es un cambio atómico o no es.
2. **Tocar `public/sw.js` obliga a bumpear `CACHE_NAME`** (hoy `robible-v28`). Sin bump, los usuarios con la PWA instalada no reciben el cambio. `public/sw.js` es la única fuente de verdad de la versión de cache: no dupliques la constante en otro sitio.
   - Y el bump no basta para que lo vean *ya*: si el usuario tiene un SW controlando la página, el nuevo se queda en `waiting` y [main.js](src/main.js) solo avisa (`robible:pwa-update-available`); es el usuario quien acepta. Purgar Cloudflare no cambia nada, porque el SW responde antes de llegar a la red.
3. **`Cache-Control: immutable` solo vale para URLs con hash en el nombre.** `/assets/*` lo lleva y está bien; `/lang/*.json` lo llevaba y estaba mal — es una URL estable cuyo contenido cambia en cada release, así que los usuarios veían la clave en crudo (`app.share.eyebrow`) en vez del texto. Y hacía más daño de lo que parece: también anulaba el `stale-while-revalidate` del service worker, cuyo fetch de refresco se resolvía contra la caché HTTP sin tocar la red y volvía a guardar los mismos bytes viejos, en bucle cerrado. Por eso ese refresco usa hoy `cache: 'reload'`. Antes de poner `immutable` en algo, pregúntate si la URL cambia de nombre al cambiar el contenido.
4. **Los códigos de versión no describen el texto.** `rvl` **no es Reina-Valera**: es la Biblia en Español Sencillo (CC BY 4.0, atribución pendiente). `vdc` es la Cornilescu *corregida*, no la de 1924. Se mantienen así por decisión del propietario; ver `public/data/CREDITS.md`.
5. **Hay cuatro Biblias y `bible.json` pesa entre 1 y 4 MB cada una.** No cargarlas en scripts ni en el arranque salvo que haga falta. La de comparación es lazy, y el SW solo precachea `vdc` y `rvl` (ver `public/data/CREDITS.md`).
6. **No se generan páginas estáticas por versículo, a propósito.** Las hubo (~31.000 por versión) y se quitaron: `dist` bajó de 628 MB a 83 MB y el build de 4 min a 13 s. Los versículos siguen siendo direccionables — `netlify.toml` enruta `/biblia/:v/:libro/:cap/:versiculo` a la función `verse-meta`, que genera las etiquetas al vuelo. No las reintroduzcas sin medir el coste.
7. **Los slugs de libro admiten cualquier alfabeto.** `slugifyBookName` filtra con `\p{L}\p{N}`, no con `[a-z0-9]`: con lo segundo los nombres chinos se vaciaban enteros y los 66 libros compartían un slug vacío.
8. **`App.svelte` tiene guardas anti-race deliberadas**: `bibleLoadRequestId`, `_localeVersionTag`, `_pendingLocale`. Parecen redundantes y no lo son — evitan que una carga vieja pise a una nueva al cambiar de versión/idioma. No simplificar.
9. **`getBibleVersionConfigOrDefault()` sin argumento devuelve siempre `vdc`**, no la versión activa. Pásale siempre `$selectedBibleVersion`. La única llamada sin argumento legítima es la de `src/config/seo.js:5`, que define a propósito la versión por defecto. Ya provocó un bug real (voz rumana leyendo español), ver auditoría hallazgo 1.
10. **`Result.svelte` tiene 2880 líneas.** Es la vista de lectura y concentra swipe, favoritos, notas, topics, TTS y SEO. Buscar por los marcadores `// === SECCIÓN ===` antes de leerlo entero.
11. **No hay router.** Añadir una ruta implica tocar `Main.svelte` (detección), `bible-versions.js` (el path por idioma), `AppMenu.svelte` (navegación) y `generate-seo.mjs` (sitemap). Excepción deliberada: `/tema/:slug` **no** pasa por `bible-versions.js`. Es un enlace que se reparte por fuera y tiene que abrir igual para quien lo reciba; con un path por idioma el mismo tema tendría varias URLs y el enlace se rompería al cambiar de versión.
12. **Cada reproducción de música cuelga de su propio nodo de ganancia** (`voiceGain` en `music.service.js`); `musicGain` es **sólo** el volumen del usuario y no se toca al arrancar o parar. Cuando todo colgaba del mismo nodo, `stop()` programaba un desvanecido de un segundo y el `play()` siguiente lo cancelaba para hacer su propio fundido: la pista vieja seguía sonando —y subiendo— encima de la nueva, y cambiar de ambiente parecía no hacer nada. Por lo mismo, el apagado se remata con `setTimeout` y no con `node.stop(cuando)`: al pausar se llama a `suspend()`, el reloj del contexto se congela y un stop programado sobre ese reloj **no llega nunca**.
13. **El `BookDrawer` va por encima de los modales** (z-index 120/121 contra 110). Se abre también desde *dentro* de un diálogo —al crear una predicación se elige el libro sin salir de él—, y por debajo quedaba desenfocado por el velo, sin recibir clics, y al pulsar un libro el clic caía en el velo y cerraba el diálogo. Tres síntomas, un solo fallo de apilado. Por encima queda sólo el Modo Amvon (200), que debe tapar todo.
14. **La lectura no tiene voz, por decisión de producto**: es música + resaltado visual, y el avance va por temporizador. `TtsPlayer` recibe `playlist` (lo que hay en pantalla), no un capítulo. Todo lo que se llama `tts` —el store, el componente, las claves `app.tts.*` y las de localStorage `robible:tts:*`— es herencia de una versión con `SpeechSynthesis` que se retiró; renombrarlo es un cambio atómico o no es. `tts.service.js` se borró el 5 sep 2026: si buscas la síntesis de voz, está en el historial de git, no en el árbol. No la reconectes sin decisión de producto: la auditoría la levantó como bug y no lo era.
15. **El sistema de diseño vive en `public/global.css`** y es la única fuente de verdad; la landing consume esos mismos tokens. Cuatro capas: escalas crudas (`--grey-*`, `--blue-*`, `--green-*`) → **paleta activa** (`--color-page`, `--color-surface`, `--color-ink`…) → derivados (`--wash-*`, `--color-line*`, que se calculan solos desde `--color-ink` y `--color-accent`) → alias heredados (`--color-blue`, `--color-white`…). **Usa la paleta y los derivados**; los heredados solo existen por los ~500 usos ya escritos.
    - **Hay cinco paletas, no un interruptor**: `html[data-theme]` vale `lumina` | `noapte` | `sepia` | `minimal` | `nocturn`. **Ningún componente lleva reglas por tema.** Si escribes `html[data-theme='…']` en un componente, estás arreglando una paleta y rompiendo las otras cuatro: lo que falta es un token.
    - Los bordes y las veladuras se mezclan **contra la tinta**, no contra el negro ni el blanco: `color-mix(in srgb, var(--color-ink) 14%, transparent)` da el valor correcto en las cinco porque `--color-ink` cambia con la paleta. Por eso `--color-line`, `--wash-hover` y compañía no se redefinen en ningún bloque.
    - **Un token que falta en una paleta no falla: hereda el de LUMINĂ.** El síntoma es un panel blanco en mitad de nocturn, y sólo se ve abriendo la app y cambiando de paleta una a una. `tests/palettes.test.js` compara los cinco bloques y ya cazó dos huecos reales (`--color-accent-ink`) el día que se escribió.
    - `--color-white` **no** sirve como color de texto: significa "fondo de tarjeta" (`--color-surface`). Para texto sobre el acento es `--color-on-primary`. Estaban mezclados en 17 sitios y en oscuro pintaban gris marengo sobre azul.
    - **`@media (prefers-color-scheme: dark)` no vale para nada aquí.** Responde al sistema operativo, no a la paleta que ha elegido el usuario: con el sistema en oscuro y Sepia puesta, la regla se aplica igual. Ya mordió dos veces —el fondo del player en la fase 4 y todo su panel en la 7, donde dejó el desplegable y los botones de velocidad invisibles— porque una limpieza parcial no las cazó todas. Si necesitas distinguir claro de oscuro, es un token.
    - **Un token de borde no es un color de texto.** `--color-line` es una tinta al 14 %; usarlo en `color:` da texto casi transparente. Era lo que dejaba el desplegable del player en blanco.
    - **Ojo con `var(--inexistente, var(--real))`.** Había tres tokens que no existían (`--color-text`, `--color-text-secondary`, `--border-color-dark`) escritos así: siempre caían al fallback, así que el primero era decorado y parecía que había un sistema donde no lo había.
    - **Una superficie que fija su fondo tiene que fijar también su color de texto.** El sidebar pintaba `--color-sidebar` (oscuro en las cinco paletas) y dejaba que el texto heredara la tinta del `body`, que es oscura: los títulos de sección salían casi negros sobre casi negro. Para eso está `--color-on-sidebar`.
    - **El acento tiene dos tokens porque tiene dos trabajos.** `--color-accent` vale para bordes e iconos, donde el mínimo es 3:1. Como **fondo bajo texto** no vale: el azul de la casa da 3.30:1 con blanco y AA pide 4.5:1. Para eso está `--color-accent-solid` (+ `--color-accent-solid-hover`), que es el mismo acento oscurecido lo justo. **Todo `background` de acento que lleve texto encima usa el token de relleno.** Se prefirió esto a oscurecer `--color-accent`, que habría cambiado el azul de RoBible en toda la aplicación.
    - **En las paletas oscuras el acento es claro, así que `--color-on-primary` es oscuro.** Significa exactamente "texto sobre `--color-accent-solid`" y nada más; sobre el chrome del sidebar va `--color-on-sidebar`. Estuvieron mezclados y en Noapte eso daba 2.38:1.
    - `tests/contrast.test.js` calcula el contraste WCAG de 27 pares por paleta. No es cosmético: encontró siete fallos reales en cuatro de las cinco paletas la primera vez que se ejecutó, incluidos dos que venían de antes de que hubiera paletas. **Comprueba que el token sea correcto, no que el componente use el token correcto** — para eso hace falta mirar el DOM renderizado.
    - **`accent-color` no sirve para los `input[type=range]`.** Parece la solución de una línea y Chromium, al fijarlo, pinta la parte sin rellenar de la pista **casi negra**: sobre fondo claro se ve una franja negra a la derecha del pulgar. `global.css` los dibuja a mano; el relleno se pasa en `--range-fill`.
    - **El player publica su altura en `--player-offset`** (medida con `ResizeObserver`, no escrita a mano: la barra crece al abrir el panel). El botón de subir y los de modo lectura la usan para apartarse. Antes había un `bottom: 3.5rem !important` con el valor a ojo que además ganaba a cualquier arreglo posterior.
    - **Abajo del todo hay tres controles fijos** —salir del modo lectura, leer con música y subir— y el pie **reserva su franja** como relleno inferior (`--floating-band` + `--player-offset`). Sin esa reserva, al llegar al final de la página se plantaban encima de «Autentificare» y del selector de paleta, que quedaban intocables. El orden de abajo arriba es: player → flotantes → botones del pie. Si añades otro control fijo abajo, súmalo a la franja.
16. **Colores con significado fijo**: verde = versículo en lectura (`--color-success`) y el botón que lo activa; ámbar = favorito; el color del tema = índice temático; el color elegido = subrayado. Los siete iconos del versículo comparten `.icon-btn` y el estado `.icon-btn--marked`, que solo lee `--marked-color`. Para un icono nuevo, añade un modificador que fije esa variable — no dupliques el bloque de estilos.
17. **La paleta de subrayado se salta la franja verde-turquesa a propósito** (`src/config/highlight-palette.js`). El verde ya significa "versículo en lectura", y el subrayado se pinta como un lavado del fondo al 26 %: a esa intensidad un turquesa y el verde de lectura no se distinguen. Por lo mismo son cinco colores y no seis — en el arco que queda, seis dejaban el amarillo y el naranja a 16°, indistinguibles como lavado. `tests/highlights.test.js` vigila ambas cosas, así que si añades un color y falla, es esto.
18. **El estado de lectura tiene que ganarle al subrayado del usuario.** `.highlight-verse` vive en `global.css` con un solo selector de clase; el scoping de Svelte le añade una clase más a `.verse--user-highlight`, así que sin la regla explícita `.verse--user-highlight:global(.highlight-verse)` de `Result.svelte` el subrayado tapaba el verde de lectura.
19. **La imagen para compartir se genera al cambiar la vista previa, no al pulsar el botón** (`VerseImageModal.svelte`). iOS Safari exige que `navigator.share` salga del gesto del usuario, y un `await canvas.toBlob()` por medio rompe esa condición: la hoja de compartir no llega a abrirse y acaba descargando el PNG.
20. **Diálogos**: usa `src/components/Modal.svelte` (centrado, hoja inferior en móvil, Escape y clic fuera). En móvil la hoja ocupa 92dvh fijos para que el teclado no la encoja al escribir; si el diálogo solo muestra algo corto, pásale `fitContent` o queda medio vacío. No vuelvas a anclar popups al botón con `getBoundingClientRect()`: se recortaban contra el borde en móvil.
21. **En la landing, las clases que añade JS necesitan `:global()`**. Svelte poda como CSS muerto lo que no ve en la plantilla: por eso la animación de aparición (`.is-visible`) nunca funcionó hasta que se envolvió en `:global()`.
22. **Los comentarios XML de los SVG no pueden contener `--`**: `sharp` revienta al leerlos. Por eso los nombres de token en `logo.svg` se escriben `(grey 800)`. Tras tocar un SVG del logo, `node scripts/build-logo.js` y bumpea el SW.
23. **Svelte sólo reacciona a lo que ve escrito en la plantilla.** Envolver una condición en un helper —`{#if isVerseSelected(item.key)}` en lugar de `{#if selectedVerseKey === item.key}`— le esconde la dependencia al compilador y deja de repintar: el estado cambia y la pantalla no. Pasó con la selección de versículo. Es el mismo motivo del `void $topicsStore` que verás en un `{@const}` de `Result.svelte`. Compara contra la variable directamente.
24. **Cuando una acción tiene color propio, ese color sustituye al azul de estado activo; no se mezclan.** Esto costó un bug que sólo se veía en oscuro: `:global(html[data-theme='dark']) .icon-btn` tenía tres clases de especificidad y `.icon-btn--marked` sólo dos, así que el icono marcado perdía su color y volvía al azul mientras el borde sí lo conservaba. Se resolvió con `:not(.icon-btn--marked)` y **hoy el problema ya no existe**: al pasar a cinco paletas desapareció la regla por tema, y sin regla por tema no hay nada que pisar el color de la acción. No la reintroduzcas.
25. **El buscador de referencias acepta prefijos desde 2 letras** (`isPlausiblePrefix`). Antes medía la diferencia de longitud contra el nombre del libro y descartaba todo lo que sobrepasara 4 caracteres, lo que tumbaba las abreviaturas más escritas: `prov`, `deut`, `apoc`, `fapte`… `prov 3 4` no devolvía nada. El umbral está en 2 y no en 1 porque con una sola letra el peor caso ("i") empareja 14 libros. Cubierto en `tests/reference-search.test.js` contra el mapa real.
26. **El store `filter` guarda una copia, no el objeto que le pasas** (`stores.js`). Sidebar hace `bind:value={searchForm.searchText}` sobre su propio objeto y luego lo mete en el store; Result lee `$: searchForm = $filter`. Cuando el store se quedaba con la referencia, los dos componentes compartían el MISMO objeto y teclear en el buscador mutaba el estado de Result por la espalda, sin notificar al store. El síntoma: buscabas por referencia, borrabas con el aspa, escribías otra vez y al pulsar la sugerencia cambiaba la URL pero no el texto. **No devuelvas `filter` a un `writable` pelado.** Cubierto en `tests/filter-store.test.js`.
27. **La respuesta de seguridad se normaliza en un solo sitio** (`normalizeSecurityAnswer` en `workers/robible-api/src/utils.js`). La usan el registro **y** la verificación al recuperar la cuenta. Lo que se guarda es un hash, así que si las dos rutas normalizaran distinto el usuario escribiría la respuesta correcta y no entraría jamás — y no habría forma de diagnosticarlo desde fuera. No dupliques esa lógica ni la cambies en una sola de las dos rutas.
28. **Los iconos son de Phosphor (MIT) y están copiados al árbol, no instalados.** Los trazos viven en `src/components/Icon.svelte`, generado por `scripts/build-icons.mjs`; no hay dependencia en tiempo de ejecución. Para añadir uno, sigue las instrucciones de la cabecera de ese script y de `public/data/CREDITS.md` — **no** metas `<svg>` a mano en un componente, que es de donde venían los 94 sueltos que había.
    - `<Icon name="x" weight="fill" />` es el patrón de iOS: mismo dibujo en contorno cuando la acción está inactiva y macizo cuando está activa. Sustituye a `getFilledTopicIconSvg`, que quitaba `fill="none"` de una cadena con un `replace`.
    - **Las claves de `src/config/topic-icons.js` están guardadas en D1** (columna `icon` de `topics`). Se pueden añadir; renombrar o quitar una deja sin icono a los temas que ya la usaban y no hay forma de recuperarlo.
    - `class:` es una directiva de elemento: sobre `<Icon>` no compila. Si necesitas alternar una clase (girar un chevron, por ejemplo), ponla en un `<span>` envolvente.
    - **El tamaño se fija con `--icon-size` en el contenedor**, no con una regla `.mi-boton svg { width: … }`: el scoping de Svelte le pone al `<svg>` la clase de `Icon.svelte`, así que esa regla no le alcanza. Es un fallo silencioso —el icono simplemente vuelve al tamaño por defecto— y en la migración dejó **17 reglas muertas**; sólo se vio porque el build avisa de selectores sin usar. Ojo también con el `max-width: 100%` que `global.css` da a todo `svg`: recorta el ancho y no el alto, y dentro de un botón estrecho dejaba el icono a 13×16. Por eso `Icon.svelte` lleva `max-width: none`.
    - **`size` es una medida CSS, no un número.** `size="18"` produce `width: 18`, que el navegador descarta, y el icono se dibuja a su tamaño intrínseco — enorme. Reventó los dos botones de la landing, que quedaron con el texto a una letra por línea. `Icon.svelte` convierte ahora un número suelto a píxeles, pero escribe la unidad igualmente. El fallo venía de la migración: el `width="18"` del SVG original sí era válido **como atributo**, y al pasar a CSS dejó de serlo.
29. **El cristal esmerilado es sólo para superficies fijas.** Navbar no, porque no es sticky y se va con el scroll; versículos y tarjetas de lista tampoco, porque cada capa con `backdrop-filter` se recompone en cada fotograma y un capítulo largo son 40-80 capas. La transparencia va **dentro** de `@supports (backdrop-filter: …)` con el fondo opaco como base: sin desenfoque, un fondo translúcido deja leer el texto de la página a través del panel. Y el Modo Amvon queda fuera a propósito: en el púlpito la legibilidad no se negocia.
30. **El Modo Amvon no hace ni una petición de red, y eso condiciona cómo se guarda.** `sermon-pulpit.service.js` deja en el dispositivo el **texto** de cada referencia citada, no sus coordenadas: resolverlas contra la Biblia en memoria funcionaría casi siempre, y "casi siempre" no vale delante de una congregación. Una referencia que no se pudo resolver se guarda con texto vacío en lugar de omitirse, para que el botón siga donde el predicador lo espera. Es también una **capa propia a pantalla completa** (`fixed inset:0; z-index:200`) y no el modo inmersivo de `Result.svelte`: el inmersivo esconde la interfaz pero deja debajo swipe, iconos y player, y cualquiera de esos apareciendo a mitad de predicación es justo lo que no puede pasar. La recuperación tras interrupción (`getActive`, en `main.js`) caduca a las seis horas para que un Amvon olvidado no secuestre el arranque meses después.

31. **La landing no tiene tokens propios: no existe ningún `--landing-*`.** Consume directamente los semánticos de `global.css`. Y esto muerde porque `var()` de un token inexistente **no falla**: el fondo se queda transparente y el borde cae a `currentColor`, así que la tarjeta se ve «casi bien» y el fallo pasa desapercibido en una revisión rápida. Copia las reglas de `.audience__card`, que es el patrón de tarjeta de esa página. Un comentario del propio fichero afirmaba lo contrario y fue justo lo que indujo el error; ya está corregido.
32. **Lo que JavaScript añade a la landing después del montaje no se anima solo.** El `IntersectionObserver` de las apariciones (`data-reveal` → `.is-visible`) se crea en `onMount` y observa lo que existe **en ese momento**. Las tarjetas que llegan de una petición nacen después: nadie las observaba y se quedaban en `opacity: 0` — la sección de predicaciones salía con el título y un hueco en blanco debajo. Tras pintar contenido nuevo hay que volver a llamar a `revelar()`. Es primo de la trampa 19: en la landing, todo lo que no está escrito en la plantilla necesita un empujón explícito.
33. **Los asteriscos del marcado manual (`*palabra*`) no pueden salir a pantalla.** Son la sintaxis con la que el predicador señala qué palabras quiere en la schiță. Se cortan en origen, en `generateOutline`, y se limpian con `quitarMarcas()` en **cada** punto de presentación: vista pública, Modo Amvon, documento final y los dos PDF. `netlify/functions/sermon-meta.mjs` duplica la expresión a propósito (`sinMarcas`) porque es una función de Netlify y no puede importar del bundle. Se escapaban por los seis sitios a la vez y se veían en una predicación publicada. Cubierto en `tests/sermon-content.test.js`.
34. **El test de i18n comprobaba la paridad entre idiomas, no la existencia.** Una clave mal escrita en el código y ausente en los cuatro ficheros cuadraba perfectamente y salía en crudo en pantalla (pasó con `app.topics.share.copy`, que se llama `copy_link`). Hay ahora un segundo test en `tests/i18n-keys.test.js` que recorre `src/` buscando `$_('literal')` y exige que exista. Sólo ve literales estáticos: si concatenas un sufijo dinámico, el prefijo acaba en `.` y se salta — esa parte sigue sin red.
35. **`/versete/<slug>` tiene un solo dueño, y antes tuvo dos.** Las colecciones curadas salen de `public/data/curated-topics.json` (lo genera `scripts/build-curated-topics.mjs`) y las pinta `CuratedTopic.svelte`. Pero cuatro de esos slugs —`dragoste`, `speranta`, `credinta`, `casatorie`— existían antes como páginas estáticas dentro de la constante `TOPICS` de `generate-seo.mjs`, con `staticOnly: true`, es decir **sin la aplicación detrás**. Mientras convivieron, el fichero estático se servía bien al entrar por URL, pero al llegar a esa misma dirección navegando dentro de la aplicación el slug no estaba en el JSON y salía «colección no encontrada». Se han mudado al JSON con la **misma URL y los mismos versículos**, porque ya estaban indexadas; `tests/curated-topics.test.js` vigila que nadie los renombre. Las cuatro hermanas en español siguen en `TOPICS` bajo `/versiculos/`, que esta ruta no atiende. Si añades una colección nueva, va al JSON, nunca a `TOPICS`.
    - La página se indexa (al revés que `/tema/<slug>`), y por eso el estado «no encontrada» lleva `noindex` explícito: devuelve 200, así que sin él Google se indexaría un soft-404 por cada slug inventado.
    - Un `icon` que no esté en `topic-icons.js` no falla: `resolveTopicIcon` lo cambia por el marcador y la colección sale publicada con el icono de otra cosa. Lo cazan el generador y el test.
    - **Navegar de `/versete/x` a `/versete/y` no vuelve a montar el componente.** El tipo de ruta no cambia, así que `Main.svelte` sigue pintando el mismo `CuratedTopic` y **`onMount` no se ejecuta otra vez**: la URL cambiaba y la pantalla se quedaba igual, con lo que los chips de «otras colecciones» parecían enlaces muertos. Por eso la carga vive en `cargar()` y se engancha además a `popstate` y a `robibile:navigate`, con una guarda de slug porque los dos eventos llegan juntos. `PublicTopic.svelte` ya lo hacía así; cualquier pantalla que enlace **dentro de su propia forma de ruta** necesita lo mismo.
36. **El frontend y el worker se despliegan por separado, y el frontend lo subes tú.** Netlify va por un lado y Cloudflare por otro, así que siempre hay una ventana en la que el cliente pide una ruta que el worker todavía no tiene. El worker responde 404 con el código genérico `not_found`; las rutas que sí existen devuelven su propio código (`memorization_not_found`, `subscription_not_found`…). Un servicio que trate todo 4xx igual **no cae a `localStorage`** y la función parece rota para quien tenga sesión iniciada. Ver `rutaNoDesplegada()` en `memorize.service.js`, cubierto en `tests/memorize.test.js`.
37. **El aviso diario por push va sin contenido, a propósito.** Mandar datos dentro de un push obliga a cifrar el cuerpo con aes128gcm (RFC 8291) dentro del worker. No hace falta: el versículo del día es determinista a partir de la fecha y `daily-verses.json` está precacheado, así que el push sólo dice «despierta» y el service worker arma la notificación solo. Consecuencia: **`public/sw.js` tiene copiada la aritmética de `daily-verse.service.js`** —un service worker clásico no comparte módulos con el bundle— y si cambia una hay que cambiar la otra, o la notificación anunciará un versículo y la aplicación abrirá otro. `tests/sw-daily-verse.test.js` compara las dos, día a día, durante 400 días.
    - El service worker no tiene `localStorage`, y cuando llega un push lo normal es que no haya ninguna pestaña abierta a la que preguntar. La versión bíblica y el idioma se le dejan en la **Cache API**, en `/__robible/push-prefs` (ver `push.service.js`).
    - Cada dispositivo guarda su hora de aviso **ya convertida a UTC**, calculada en el cliente y reenviada en cada arranque. Así el cron es una consulta indexada por hora, sin aritmética de zonas horarias en el worker, y el cambio de horario de verano se corrige solo.
    - **En iOS sólo funciona con la PWA instalada** (iOS 16.4+). En Safari como pestaña `Notification` no existe: por eso la tarjeta del perfil comprueba el soporte antes de ofrecer nada.
    - Sin `VAPID_PUBLIC_KEY` configurada, el botón avisa de que no está disponible y **no llega a pedir permiso**. Importa: un permiso denegado no se puede volver a pedir desde el código.

35. **`/predici` es el blog PÚBLICO; el cuaderno privado es `/predicile-mele`.** Se cambió el 8 sep 2026 y es fácil de confundir porque el menú «Predicile mele» apunta al segundo. La lógica: singular una (`/predica/<slug>`), plural todas (`/predici`), y lo privado lleva su nombre completo. Las tres rutas se detectan en `Main.svelte` y ninguna se traduce por idioma — las públicas porque se comparten e indexan, la privada porque no gana nada teniendo cuatro formas.
36. **La guía de homilética sale de un curso, no de la cabeza de nadie.** `src/config/homiletics.js` sólo tiene el esqueleto (cuántas viñetas por paso); el texto vive en `app.homiletics.*` de los cuatro ficheros de idioma. Viene del curso de predicación expositiva del Institutul Teologic Penticostal din București, y el ejemplo es siempre Tito 2:11-14 a propósito: ver la misma predicación avanzar paso a paso enseña más que siete ejemplos sueltos. Si añades una viñeta, sube el contador en `GUIA` **y** añade la clave en los cuatro idiomas.
37. **El paso IDEE tiene cuatro campos y el orden importa**: `exegetical` (lo que el texto dijo a SUS destinatarios) → `purpose` (lo que Dios quiere cambiar hoy) → `central` (la idea homilética) → `question` (la pregunta analítica de la que salen las divisiones). La clave `central` **no** se renombró aunque su etiqueta cambiara a «idea homilética»: está usada en el PDF, la vista pública y la landing.
38. **Un `textarea` que edita un array no puede re-derivar su texto del array mientras se escribe.** La schiță edita las palabras clave como lista de texto, y el valor del campo es estado propio (`textoClaves`) que sólo se sincroniza al abrir o al regenerar. Derivándolo, una línea vacía a media frase desaparece al parsear y el cursor salta al final en cada tecla.
39. **El módulo de predicación no lleva botón flotante de modo lectura.** El inmersivo esconde el cromo para *leer la Biblia*; sobre un formulario de preparación no significa nada, y se plantaba encima del texto del guía. Está excluido en `Main.svelte` con `!isSermonsMode`.
40. **No hay motor de plurales.** El par singular/plural se elige en el punto de llamada (`count === 1 ? $_('…verse_count') : $_('…verses_count_plural')`). Escribir una sola clave da «1 predici» y «1 puncte» en rumano, que ya han salido a pantalla dos veces.

41. **Hay tres pares singular/plural de rutas públicas y conviene no mezclarlos**: `/tema/<slug>`–`/teme`, `/predica/<slug>`–`/predici`, y el privado `/predicile-mele`. Las de temas van con **`noindex, follow`** y las de predicaciones con **`index, follow`**: un tema es una lista de versículos que ya tienen página propia (duplicado y fino), mientras que una predicación es texto original que su autor publica para que se encuentre. Por eso `/teme` **no** está en el sitemap y `/predici` sí.
42. **El tipo de predicación (`expositive` | `textual` | `thematic`) sí hace algo desde el 8 sep 2026.** Antes se guardaba y se pintaba en la lista, nada más. Ahora cambia los avisos del guía en **los siete pasos** (`app.homiletics.types.*`), y sólo para los dos tipos que no son expositiva — la guía entera está escrita para ésa. Empezó cubriendo tres pasos y se extendió a los siete el 10 sep 2026: quedarse a medias era peor que no estar, porque en las otras cuatro pantallas se leía la guía de un curso de expositiva sin que nada lo advirtiera. Si añades un tipo, hay que tocar `SERMON_TYPES` en el worker **y** en `sermons.service.js`, más las claves `type_*`, `type_*_hint`, las siete de `types.<tipo>.*` y, si procede, `TIPOS_CON_NOTA` de `homiletics.js`.
    - El parecido entre los avisos de textual y los de expositiva **es correcto, no un descuido**. La clasificación en tres especies viene de Broadus y el eje que las separa es de dónde salen las divisiones; textual y expositiva comparten ese principio y se diferencian sobre todo en la longitud del pasaje. Verificado contra fuentes de homilética el 10 sep 2026.
43. **Los enlaces públicos del pie están en dos sitios y son distintos.** `Footer.svelte` (la aplicación) los saca de `ENLACES_PUBLICOS`, un array de tres líneas; la landing tiene su propio `.footer__nav` escrito a mano, que reutiliza las claves `app.footer.links.*` a propósito para no traducir lo mismo dos veces. Al añadir una sección pública hay que acordarse de los dos.

44. **Regenerar la schiță FUNDE, no rehace** (`mergeOutline`). La schiță guarda `base`, una instantánea de la última generación, y al regenerar se comparan tres estados —`base`, lo que hay ahora y lo que se generaría hoy— campo a campo: igual que `base` significa que nadie lo tocó y se refresca; distinto significa que lo escribió el predicador y se respeta. Es el modelo de git. **Si tocas `generateOutline`, acuérdate de que devuelve su propia `base`**: sin ella la primera regeneración no puede distinguir nada. Y una schiță sin `base` (las de antes del 8 sep 2026) no se pisa nunca: sólo se rellenan huecos vacíos. Cubierto en `tests/sermon-merge.test.js`, incluida la estabilidad — regenerar dos veces seguidas sin tocar nada tiene que dar el mismo objeto.
45. **`sermon` se carga una vez en `onMount` de `SermonPrep` y NO se refresca.** `sermon.outline` y `sermon.content` son la foto del momento de abrir la pantalla; el estado vivo son las variables `outline` y `content`. Leer de `sermon` en un manejador tira todo lo editado en la sesión — ya pasó con `crearSchita`, que en silencio devolvía la schiță al estado de la carga cada vez que se pulsaba «Creează schița».

46. **El chino no se parte por espacios, porque no los tiene.** `wrapTextLines` de `verse-image.service.js` hacía `split(/\s+/)`, así que un versículo chino salía como una sola «palabra» y por tanto como una sola línea: en la imagen para compartir se veía una tira con el principio y el final fuera del lienzo. Ahora cada ideograma es una oportunidad de corte propia y hay kinsoku básico (`。、」` no abren línea; `「（` no la cierran). El truco es que el troceador devuelve cada pieza **con el separador que le toca delante** —espacio entre palabras latinas, nada entre ideogramas—, así que el mismo algoritmo sirve para las cinco lenguas sin ramas por idioma. Cubierto en `tests/verse-image.test.js`, incluido que el latín se parte exactamente igual que antes.
47. **Los fondos de la imagen para compartir tienen dos capas de personalización.** Los seis primeros comparten el pintor `gradient` y sólo cambian de paleta; `aurora`, `rays` y `arcs` traen `style` y su propio pintor en `PINTORES`. Si añades uno con dibujo propio, dale también `swatch` (CSS): la muestra del selector se dibuja con `stops` por defecto, y dos fondos oscuros distintos saldrían como el mismo rectángulo. La viñeta final es común a todos y vive fuera de los pintores.

48. **En la preparación hay CUATRO bloques plegables y no son lo mismo.** `Ajutor.svelte` enseña homilética (veladura de acento, `robible:sermons:guide-open`); `Recapitulare.svelte` recuerda lo que ya has respondido en los pasos anteriores (superficie con filete de acento, `robible:sermons:recap-open`); `Notite.svelte` enseña tus notas de trabajo (superficie hundida con filete en tinta suave, `robible:sermons:notes-open`); `Sugestii.svelte` es la bombilla de la frase de transición y vive **dentro** de su campo (`robible:sermons:hint-open`). Cuatro preferencias separadas a propósito: quien ya sabe homilética cierra la primera y deja abierta la segunda. Los tres primeros son sólo lectura; el único que hace algo al pulsarlo es el cuarto.
    - Las que salen de salida son el guía y la recapitulación. Las notas y la bombilla nacen **cerradas**: son bloques largos y abiertos empujarían el campo del paso fuera de la pantalla en un móvil, que es justo lo que se venía a evitar.
    - La recapitulación sólo sale en `structure`, `development` y `final` —antes no hay nada que recordar— y sólo lista campos con algo escrito. Las notas salen en los seis pasos posteriores a `text`, que es donde se escriben.
49. **El tema de una predicación se elige, no se escribe** (`SeriesPicker.svelte`). Era texto libre y salían «Predica de pe munte», «predica de pe munte» y «Pe munte» como tres series distintas, con el filtro del blog repartido entre ellas. Las sugerencias se juntan de dos sitios: las propias salen de `get(sermonsStore)` (van aunque no hayas publicado nada) y las ajenas de `GET /api/public/sermon-series`, que sólo devuelve las de predicaciones publicadas. Escribir un nombre que ya existe con otras mayúsculas reutiliza el que hay. **Sigue siendo un valor y no varios**: `series` es una columna y el blog filtra por ella; hacerlo lista pediría otra tabla.
50. **`{:else}` sobre una condición con exclusiones es una trampa.** El botón flotante de modo lectura de `Main.svelte` era `{#if !isImmersive && !isCompareMode && …}{:else}<salir del modo inmersivo>{/if}`: al excluir el módulo de predicación, esas pantallas cayeron en el `else` y mostraban el botón de SALIR de un modo en el que no se estaba. La rama de salida comprueba ahora `isImmersive` explícitamente.

51. **Un solo selector de capítulos** (`ChapterPicker.svelte`) para la lectura y la comparación. Antes había dos y los dos se rompían con los Salmos en un móvil: la lectura los sacaba en una tira horizontal de 6903 px con el capítulo activo fuera de pantalla y sin desplazarse hasta él —lo causaba un `flex-wrap: nowrap` en la media query— y la comparación metía ~25 filas en una ventana de 72 px. Ahora es una rejilla que envuelve y **centra el capítulo activo al abrir**. Dos detalles que costaron: `.capitole` necesita `position: relative` (si no, `offsetTop` se mide contra la cabecera pegajosa de la comparación) y `flex: 1 1 0`, porque allí vive dentro de una fila flex y sin eso la rejilla se encogía a UNA columna: 150 filas y 6480 px de alto.
52. **`/predici` la sirve una función de Netlify** (`sermons-index-meta.mjs`), igual que `/tema` y `/predica`. Es la página con la que se quiere aparecer al buscar «predică expozitivă», así que el rastreador recibe el método y la lista ya escritos más un `HowTo` en JSON-LD, sin depender de que ejecute JavaScript. Los siete pasos están **duplicados en rumano** dentro de la función: es una función de Netlify y no puede importar del bundle. Si cambias `app.homiletics.*`, repasa también ese archivo.
53. **Objetivos táctiles: 24 px es el mínimo** (WCAG 2.5.8). Se arreglaron los enlaces del pie (17 px), los de la landing (22), las migas de pan (19) y el aviso de scroll del hero, que era un enlace de **1×48 px** porque la línea decorativa ERA el enlace. **La excepción son los enlaces dentro de una frase**: `.reference` de los versículos se queda en 23 px a propósito, porque la norma exime los objetivos en línea y forzarle altura rompería el flujo del texto.

54. **El versículo del día tiene DOS lectores y no hacen lo mismo.** `getVerseForToday()` es la del diálogo automático: calla si ya se mostró hoy o si el usuario lo desactivó. `getTodayVerse()` no tiene condiciones y es la que usa el perfil, donde se entra a propósito a buscarlo — antes, cerrarlo o desactivarlo lo dejaba inalcanzable para siempre.
55. **`reading-progress.service.js` no se sincroniza y es deliberado.** Sólo localStorage: quien lee en el sofá y estudia en el escritorio tiene dos hilos distintos, y unificarlos le pisaría uno. En `Profile.svelte`, el `+ 1` sobre `chapter` es la conversión de índice base 0 a número de capítulo, **no** «el capítulo siguiente»: continuar significa volver adonde estabas.
56. **`topicsStore` no es una lista.** Guarda `{ topics, verseRefs }`. Contarlo con `$topicsStore.length` da `undefined` sin avisar — ya pasó en el contador del perfil. Es el único store del proyecto con esa forma.

57. **En un componente de 2000 líneas, una clase se reutiliza sin querer.** `.subpunto` ya era del paso Structură —una fila `flex` con `button { width: 1.6rem }`— y el bloque nuevo de Dezvoltare la reutilizó. Como iba después en la hoja, ganaba: el bloque salía en horizontal y «Marchează» y «+ Referință» quedaban estrujados a 1,6 rem, con el texto en vertical, una letra por línea. El scoping de Svelte **no** protege de esto: aísla el componente del resto de la aplicación, no una parte del componente de otra. Se llama `.subdesarrollo` por eso. Antes de nombrar una clase en `SermonPrep.svelte` o en `Result.svelte`, búscala.
58. **`content.development` va indexado por dos clases de id y guarda dos formas distintas.** La de un punto (`p_…`) tiene `{ explain, illustrate, apply, refs }`; la de un subpunto (`s_…`), `{ text, refs }`. No chocan porque `newPoint` y `newSubpoint` prefijan distinto, y `desarrolloDe()` decide la forma por el prefijo — que es lo que permite que marcar palabras y añadir referencias funcionen igual en los dos sin duplicar cada manejador. Si tocas los prefijos, esto se rompe en silencio: el subpunto empezaría a nacer con las tres casillas del punto.
59. **`content.notes` no sale del cuaderno, y ese es el punto.** Son los apuntes del predicador —dudas, material a medio pensar, ilustraciones ajenas— y quedan fuera del recuento de palabras, del documento, de los dos PDF, de la schiță y de la página pública. Un apunte publicado bajo su nombre es el peor fallo posible de este módulo y no habría forma de enterarse desde fuera, así que va cubierto en `tests/sermon-notes.test.js` con una cadena centinela. **Cualquier salida nueva de la predicación tiene que dejarlo fuera a propósito.** Lo contrario es `content.transition`, que sí se predica: cuenta y sale en todas.
60. **Al cerrar un editor en línea, el `blur` del campo se dispara al quitarlo del DOM.** El título de `SermonPrep` guarda en `blur` y cancela con Escape; Escape ponía `editandoTitulo = false`, Svelte quitaba el `<input>` y el navegador disparaba su `blur` — que volvía a entrar en `guardarTitulo` y guardaba justo el borrador recién descartado. Cancelar no cancelaba nada. Se arregla con una guarda `if (!editandoTitulo) return` al principio del guardado, que de paso evita que Enter guarde dos veces. Vale para cualquier campo que se cierre solo.
61. **El título de la predicación es obligatorio sólo en el cliente.** `puedeCrear` lo exige al crear y el editor en línea no deja vaciarlo, pero el worker sigue aceptando `title` nulo a propósito: endurecerlo rompería a quien tuviera el bundle viejo durante la ventana entre los dos despliegues (trampa 36), y en D1 ya hay filas sin título que se quedarían sin poder actualizarse. Las predicaciones antiguas sin título se ven como «Fără titlu» y se arreglan pulsando el título en la pantalla de preparación.
62. **`syncFromServer` ya no conserva el contenido local para siempre.** Antes, un sermón con contenido en caché se quedaba con esa copia sin importar cuánto hubiera cambiado en el servidor: dos dispositivos editando el mismo sermón nunca veían el cambio del otro, ni haciendo refresh. `contenidoSigueValido(local, remoto)`, en `sermons.service.js`, compara `updatedAt` como cadena ISO (comparación lexical, no `Date`) y sólo conserva lo local si es igual o más nuevo que lo remoto. Cubierto en `tests/sermons-sync.test.js`.
63. **El botón «Actualizează» sólo fuerza un guardado si de verdad hay uno pendiente**, y hay dos conceptos con nombre parecido que no son lo mismo: `guardadoEnElAire` (en `SermonPrep.svelte`) es true mientras un guardado *debounced* está programado y aún no ha llegado al servidor; el "pending" de `sermons.service.js` es la cola offline (sermón creado sin red). Sin el guardia `if (guardadoEnElAire) await guardarYa()`, pulsar el botón sin nada realmente pendiente subía igualmente la copia local — aunque fuera vieja — y pisaba una edición más reciente hecha un segundo antes desde otro dispositivo. Lo cazó la propia batería de pruebas de este cambio, no un aviso del usuario.
64. **El heurístico de «no partir esta página» del PDF mide el alto real, no cuenta bloques.** `cabeEnUnaPagina()` / `estimarAlto()`, en `sermon-pdf.service.js`, sustituyen a un `grupo.length <= 8` sin relación con cuánto ocupaba cada bloque: un desarrollo con explicación/ilustración/aplicación largas se declaraba «no partible» y, al no caber en lo que quedaba de página, pdfmake no lo partía — lo omitía entero. Ahora se estima el alto en puntos contra `ALTO_UTIL` (620pt) antes de decidir `unbreakable`.
65. **STEPS tiene ocho pasos y la introducción va ANTES del desarrollo**, aunque el curso de homilética que alimenta `homiletics.js` enseñe a escribirla al final. Es una decisión de producto explícita del 10 sep 2026: la preparación es lineal, y dejar la introducción para el final hacía que el predicador llegara al paso «final» sin haberla redactado nunca. `GUIA.intro` (4 viñetas) y `GUIA.final` (1, sólo la conclusión) se separaron a la vez — antes compartían un único paso con 5 viñetas.
66. **Los subpuntos de un punto van ANTES de explicación/ilustración/aplicación, no después.** Un subpunto es una subdivisión de la IDEA del punto; explicar/ilustrar/aplicar es lo que se hace una vez esa idea —con sus subdivisiones— ya está completa. Con el orden antiguo, la aplicación podía citar un subpunto que en el documento aún no había aparecido. Se cambió a la vez en `SermonPrep.svelte` (edición y «documento final»), `sermon-pdf.service.js` y `PublicSermon.svelte`, que comparten el mismo orden para que pantalla, PDF y página pública no diverjan. `explicar`/`aplicar` son obligatorios y `ilustrar` opcional (`.campo__marca` en el campo), igual que ya fija `PASOS_CON_NOTA`.
67. **Hay dos sintaxis de marcado en los campos de texto y no hacen lo mismo.** `*palabra*` (trampa 33) es negrita — el predicador señala qué remarcar en voz. `{{Referencia|texto del versículo}}` es una cita bíblica insertada en el punto exacto del textarea donde estaba el cursor, y se pinta con la referencia en negrita y el texto en cursiva. Las dos conviven en el mismo campo y las procesa `segmentarTexto()` (`sermon-content.service.js`) con un único regex mixto (`PATRON_MIXTO`), para no arriesgarse a que dos pasadas de reemplazo por separado descuadren los índices. `quitarMarcas()` aplana ambas a texto plano (`Referencia: texto`) para el recuento de palabras, las palabras clave de la schiță y el HTML sin JS de los crawlers — si añades una tercera sintaxis, tócala en los dos sitios.
68. **`paraElPublico` expone el nickname del autor, la única excepción a «nada del usuario».** El resto de la predicación pública (`getPublicSermon` / `listPublicSermons`, en `workers/robible-api/src/sermons.js`) no lleva ni id ni nickname de quien la escribió; el autor es la excepción porque el predicador la firma él mismo al publicarla. Sale como `author` vía un `JOIN users`, y `PublicSermon.svelte` / `PublicSermons.svelte` / `sermon-meta.mjs` lo pintan como firma con fecha. Un campo nuevo en la respuesta pública, por defecto, NO lleva datos de usuario: éste es la excepción, no el patrón.

## Mapa rápido

```
src/
  main.js                    arranque, redirect a /landing, registro del SW
  App.svelte                 carga de Biblia + locale, layout raíz
  config.js                  API_BASE_URL, USE_BACKEND
  config/bible-versions.js   catálogo de versiones: paths, locales, SEO
  layouts/main/              Result (lectura), Compare, Index, Favorites, Notes, Sidebar
  layouts/landing/           Landing pública (4 idiomas, ?lang=xx)
  services/                  datos (API-first), i18n, seo, music, filtros, rutas
  store/                     stores Svelte + persistencia localStorage
workers/robible-api/         backend Hono sobre Workers + D1
scripts/generate-seo.mjs     páginas SEO estáticas + sitemaps (post-build)
netlify/functions/           og-image, verse-meta, topic-meta, sermon-meta, sermons-sitemap
public/data/{vdc,rvl}/       Biblias en JSON
public/lang/{ro,es,en,zh}/   traducciones de UI
```

Detalle completo en [docs/ARQUITECTURA.md](docs/ARQUITECTURA.md). Despliegue y runbook en [docs/OPERACIONES.md](docs/OPERACIONES.md).
